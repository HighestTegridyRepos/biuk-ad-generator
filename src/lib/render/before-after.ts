// TODO: Refactor this to use the new single-overlay method
export async function renderBeforeAfterQuad(): Promise<string> {
  // const { default: sharp } = await import("sharp")
  // const bg = await sharp({ create: { width: 1080, height: 1080, channels: 4, background: "black" } }).png().toBuffer();
  // return `data:image/png;base64,${bg.toString("base64")}`
  return ""
}
